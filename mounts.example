# Make sure all mounts from Docker for Mac > Preferences > File Sharing are removed
# Only 1 path mapping per line 
# Absolute and relative paths are allowed. 
# If <remote path> is not specified, the script will use the resolved local path as the remote path.
# Comments are NOT allowed on the same line as a path mapping.
#
# examples:
#   <local path>:<remote path>
#   <local path>
#  


.:/mnt/current-proj
../other-dir:/mnt/other
/Users/kbentrup/Documents
/tmp:/tmp/mac
